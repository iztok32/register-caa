# docker-compose.prod.yml — produkcija na strežniku
# Uporaba:
#   docker compose -f docker-compose.prod.yml --env-file .env.prod pull
#   docker compose -f docker-compose.prod.yml --env-file .env.prod up -d

services:
  app:
    image: iztokvozlic/register-caa:latest
    container_name: register-caa-app
    ports:
      - "127.0.0.1:8083:80"    # dostopen samo lokalno — nginx/aaPanel proxira na 443
    environment:
      APP_NAME: "CAA - javni register"
      APP_ENV: production
      APP_KEY: ${APP_KEY}
      APP_DEBUG: "false"
      APP_URL: ${APP_URL}
      APP_LOCALE: sl
      APP_FALLBACK_LOCALE: en
      AVAILABLE_LANGUAGES: sl,en,de,it,hr
      AVAILABLE_COLOR_THEMES: zinc,amber,blue,emerald,orange
      LOG_CHANNEL: stderr
      LOG_LEVEL: warning
      DB_CONNECTION: pgsql
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      SESSION_DRIVER: database
      QUEUE_CONNECTION: database
      CACHE_STORE: database
      FILESYSTEM_DISK: local
      QUEUE_CONVERSIONS_BY_DEFAULT: "false"
      NOTIFICATION_ENABLE_PORTAL: "true"
      NOTIFICATION_ENABLE_EMAIL: "true"
      NOTIFICATION_ENABLE_SMS: "false"
      MAIL_MAILER: ${MAIL_MAILER:-smtp}
      MAIL_HOST: ${MAIL_HOST:-}
      MAIL_PORT: ${MAIL_PORT:-587}
      MAIL_USERNAME: ${MAIL_USERNAME:-}
      MAIL_PASSWORD: ${MAIL_PASSWORD:-}
      MAIL_FROM_ADDRESS: ${MAIL_FROM_ADDRESS:-noreply@register-caa.iztokv.eu}
      MAIL_FROM_NAME: "CAA - javni register"
      TRUSTED_PROXIES: "*"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - storage_app:/var/www/html/storage/app/public
      - storage_logs:/var/www/html/storage/logs
    restart: always
    networks:
      - register-caa

  postgres:
    image: postgres:16-alpine
    container_name: register-caa-postgres
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_DATABASE}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    networks:
      - register-caa

volumes:
  postgres_data:
  storage_app:
  storage_logs:

networks:
  register-caa:
    driver: bridge
